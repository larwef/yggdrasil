package experimental

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

// Just a simple test to make sure the test setup is working.
func TestExperimental(t *testing.T) {
	assert.True(t, true)
	// assert.True(t, false)
}
